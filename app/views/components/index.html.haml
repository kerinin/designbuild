%h1 Listing components

=render :partial => 'shared/project_nav'

=render :partial => 'component_list', :locals => {:components => @components}

%br

= link_to 'New Component', new_project_component_path(@project)
