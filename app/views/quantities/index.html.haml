%h1 Listing quantities

%table
  %tr
    %th Name
    %th Value
    %th Unit
    %th Drop
    %th
    %th
    %th

  - @quantities.each do |quantity|
    %tr
      %td= quantity.name
      %td= quantity.value
      %td= quantity.unit
      %td= quantity.drop
      %td= link_to 'Show', [@project, @component, quantity]
      %td= link_to 'Edit', edit_project_component_quantity_path(@project, @component, quantity)
      %td= link_to 'Destroy', [@project, @component, quantity], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Quantity', new_project_component_quantity_path(@project, @component)
