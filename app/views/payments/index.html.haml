=render :partial => 'shared/project_nav', :locals => {:current => :invoicing}

.container
  %h1 Listing payments

  =render :partial => 'payment_list'

  %br

  = link_to 'New Payment', new_payment_path
