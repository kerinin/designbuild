%h1 Listing fixed_cost_estimates

%table
  %tr
    %th Name
    %th Cost
    %th
    %th
    %th

  - @fixed_cost_estimates.each do |fixed_cost_estimate|
    %tr
      %td= fixed_cost_estimate.name
      %td= fixed_cost_estimate.cost
      %td= link_to 'Show', [@project, @component, fixed_cost_estimate]
      %td= link_to 'Edit', edit_project_component_fixed_cost_estimate_path(@project, @component, fixed_cost_estimate)
      %td= link_to 'Destroy', [@project, @component, fixed_cost_estimate], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Fixed cost estimate', new_project_component_fixed_cost_estimate_path(@project, @component)
