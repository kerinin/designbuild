%h1 Listing fixed_cost_estimates

%table
  %tr
    %th Name
    %th Cost
    %th
    %th
    %th

  - @fixed_cost_estimates.each do |fixed_cost_estimate|
    %tr
      %td= fixed_cost_estimate.name
      %td= fixed_cost_estimate.cost
      %td= link_to 'Show', fixed_cost_estimate
      %td= link_to 'Edit', edit_fixed_cost_estimate_path(fixed_cost_estimate)
      %td= link_to 'Destroy', fixed_cost_estimate, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Fixed cost estimate', new_fixed_cost_estimate_path
