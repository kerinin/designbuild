<% if @fixed_cost_estimate.errors.any? %>

  <% content_for :error_tr do %>
    <tr class='error'><td colspan = 10><strong><%= error_messages_for(@fixed_cost_estimate) %></strong></td></tr>
  <% end %>

  //Hide the flash notice div
  $("#flash_notice").hide(300);

  //Add a table row above the input with error info
  $("tr.error").remove();
  $("tr.fixed_cost_estimate_fields").before("<%= escape_javascript( yield :error_tr ) %>");

<% else %>

  location.reload();
  
<% end %>
