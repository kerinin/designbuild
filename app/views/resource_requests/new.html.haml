=render :partial => 'shared/project_nav', :locals => {:current => :resources}

.container

  %h1="New Request for #{@project.name}"

  = render 'form'

  = link_to 'Back', project_resource_requests_path(@project)
