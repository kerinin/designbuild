-content_for :head do
  = javascript_include_tag 'jquery.tablednd.js'
  =render :partial => 'dnd.js'

=render :partial => 'shared/project_nav'

%h1 Fixed-Bid Contracts

%hr

=render :partial => 'contract_list', :locals => {:contracts => @contracts}
