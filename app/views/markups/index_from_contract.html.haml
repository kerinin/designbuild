-@contract = @parent
-@project = @contract.project
-@component = @contract.component
=render :partial => 'shared/project_nav', :locals => {:current => :contracts}

.container
  =render :partial => 'contracts/nav', :locals => {:current => :markups}

  %h1 Markups

  =render :partial => 'markup_lines', :locals => {:markups => @markups}
  
  =add_button 'Add Markup', new_contract_markup_path(@contract)
