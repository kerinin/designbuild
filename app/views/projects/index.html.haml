%h1 Projects

%hr

%table
  %tr
    %th Name
    %th.currency Cost Estimate
    %th.currency Cost-to-Date
    %th.control
    %th.control
    %th.control

  - @projects.each do |project|
    %tr
      %td= project.name
      %td.currency= number_to_currency(project.estimated_cost)
      %td.currency= number_to_currency(project.cost)
      %td.control= link_to 'Show', project
      %td.control= link_to 'Rename', edit_project_path(project)
      %td.control= link_to 'Destroy', project, :confirm => 'Are you sure?', :method => :delete
      
  - if @projects.empty?
    %tr
      %td{:colspan => 10} (None)
