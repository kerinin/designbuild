%h1 Listing projects

%table
  %tr
    %th Name
    %th.currency Cost Estimate
    %th.currency Cost-to-Date
    %th.control
    %th.control
    %th.control

  - @projects.each do |project|
    %tr
      %td= project.name
      %td= number_to_currency(project.estimated_cost)
      %td= number_to_currency(project.cost)
      %td= link_to 'Show', project_components_path(project)
      %td= link_to 'Edit', edit_project_path(project)
      %td= link_to 'Destroy', project, :confirm => 'Are you sure?', :method => :delete
      
  - if @projects.empty?
    %tr
      %td{:colspan => 10} (None)

%br

= link_to 'New Project', new_project_path
