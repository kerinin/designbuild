%h1 Listing unit_cost_estimates

%table
  %tr
    %th Name
    %th Unit cost
    %th Tax
    %th
    %th
    %th

  - @unit_cost_estimates.each do |unit_cost_estimate|
    %tr
      %td= unit_cost_estimate.name
      %td= unit_cost_estimate.unit_cost
      %td= unit_cost_estimate.tax
      %td= link_to 'Show', unit_cost_estimate
      %td= link_to 'Edit', edit_unit_cost_estimate_path(unit_cost_estimate)
      %td= link_to 'Destroy', unit_cost_estimate, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Unit cost estimate', new_unit_cost_estimate_path
